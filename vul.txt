def vul_1(text, report, version, i_ver):
    program = ''
    i = 1
    check = 0


    for str_old in text.splitlines():
        str_new = str_old  # str - с комментариями, s - без комментариев
        s_old = ''
        s = ''
        # Формирование s_old и str:
        if check == 0:
            s_old, s = comment1(str_new)
        check, s_old, s = comment2(check, str_new)

        # Проверка на уязвимость:


        str_new = str_new.replace(s_old, s, 1)  # Добавление s в новую строку
        program += str_new + '\n'  # Добавление новой строки в текст программы
        i += 1

    return report, program, i_ver